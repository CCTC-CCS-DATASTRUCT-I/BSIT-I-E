#include <iostream>

using namespace std;

int main() {
    int y;
    int z;
    cout << "Enter first number: ";
    cin >> y;
    cout << "Enter second number: ";
    cin >> z;
    int sum = y + z;
    int diff = y - z;
    int quotient = y / z; // Corrected division operation
    cout << "sum = " << sum << endl;
    cout << "diff = " << diff << endl;
    cout << "quotient = " << quotient << endl;

 
}
