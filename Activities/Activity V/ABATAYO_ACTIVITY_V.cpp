#include <iostream>
using namespace std;

int main() {
    int age;
    cout<<"Enter age:";
    cin>>age;
     
     if(age >= 0 && age <=3) {
       cout << "Get normal ticket";
     
     } else if (age>= 6 && age <= 4); {
       cout << "get discounted tickets"

     }else{
         cout << "Normal Ticket";
    
    }
}
