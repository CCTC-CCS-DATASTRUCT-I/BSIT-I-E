#include <iostream>
using namespace;

int main() {
    int age;
    cout<<"Enter age:";
    cin>>age;
     
     if(age >= 7) {
       cout << "Get normal ticket";
     
     }
     else if (age <= 6 && age >= 4); {
       cout << "get discounted tickets"

    else if(age >= 0 && <= 3);
    cout << "get free tickets";
    
    
    }
}